<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shippers">
    <!-- Bật quyền sử dụng internet để lấy API từ heroku -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <!--<activity android:name=".login.Activity.SignInAndSignUp"></activity>-->

<!--         <activity android:name=".TestAPI"></activity>-->
        <activity
            android:name=".login.Activity.MainLogin"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--TEST-->
<!--        <activity-->
<!--            android:name=".ShippersMainActivity"-->
<!--            android:theme="@style/AppTheme2">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->
<!--        orders-->
        <activity android:name=".ShippersMainActivity"/>
        <activity
            android:name=".GetOrderActivity"
            android:label="@string/app_name" />
        <!-- Login -->
        <activity
            android:name=".login.Activity.SignInAndSignUp"
            android:theme="@style/AppTheme"
            />
        <activity android:name=".login.Activity.CreatePassword" />
        <activity android:name=".login.Activity.LoginPassword" />
        <activity android:name=".login.Activity.CreatePhoneNumber" />
        <activity android:name=".login.Activity.LoginPhoneNumber" />
        <activity android:name=".login.Activity.FormCreateAccount" />

    </application>

</manifest>